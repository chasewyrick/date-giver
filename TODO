Split the whole thing into a bunch of files
Find a way the code to be less than 80 chars
See how to do the Makefile correctly
get a file with all the strings needed and print 'em with the variables (80 ch)
translate da app

ask user to input YYYY/MM/DD
get the code for the year
if year < 2000, remove 1 to the result
check if year "bixestille", and if date is before 1st march, add 1 to result
modulo of division of the result by 7
convert dat rest in day with the following list:
0: sunday
1: monday
...
